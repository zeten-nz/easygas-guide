# EASY GAS client environment
# In development the Vite proxy forwards /api to the local server, so the
# default relative URL works without configuration.
VITE_API_URL=/api/v1
VITE_SUPPORT_URL=https://t.me/EasygasGarantbot
ajoyib=1